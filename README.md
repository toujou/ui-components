# ui-components

Mono repo with toujou's webcomponents

## Requirements
npm v16.14.0

## Install dependencies
On the root folder run:
1. `nvm use`
2. `npm run install`

## Build
On the root folder run:
- `npm run build`
