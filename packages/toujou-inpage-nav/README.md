# TOUJOU-SPINNER

The `<toujou-inpage-nav>` render an inpage navigation element that links to the set targets, updates the active item state and toggles between mobile / desktop layout according to the viewport size

## How to install
1. install via NPM with `npm install @toujou/toujou-inpage-navigation`
